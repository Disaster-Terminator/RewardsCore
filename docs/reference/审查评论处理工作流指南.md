# 审查评论处理工作流指南

> 最后更新: 2026-02-24

## 快速入门

本文档帮助新 agent 快速理解审查评论处理流程。

## 核心概念

### 操作对象 vs 参考对象

| 类型 | 模型 | 用途 | 操作 |
|------|------|------|------|
| **Thread** | `ReviewThreadState` | 主要操作对象 | 可解决、可回复 |
| **Overview** | `ReviewOverview` | 只读参考 | 仅阅读，不可解决 |
| **IssueCommentOverview** | `IssueCommentOverview` | 只读参考 | 仅阅读，不可解决 |

**重要**：Agent 主要操作 Thread 数据。Overview 用于了解 PR 整体评价和高层建议。

### 三种 AI 审查工具

| 工具 | GitHub 用户名 | 特点 | 解决方式 |
|------|--------------|------|----------|
| **Sourcery** | `sourcery-ai bot` | 结构化摘要（Prompt for AI Agents） | `@sourcery-ai resolve` 或 API |
| **Qodo** | `qodo-code-review bot` | PR Reviewer Guide（Issue Comment） | 仅 API |
| **Copilot** | `Copilot AI` | 简单评论，无结构化摘要 | 仅 API |

## ⚠️ 最常见的错误：混淆代码变化摘要和改进意见

### 代码变化摘要（不是改进意见！）

以下内容**不需要处理**，只是描述 PR 做了什么：

- **Sourcery**: "Reviewer's Guide"
- **Qodo**: "Review Summary by Qodo"

### 改进意见摘要（需要处理！）

以下内容包含具体的改进建议：

- **Sourcery**: "Prompt for AI Agents"
- **Qodo**: "PR Reviewer Guide 🔍"

## enriched_context 字段

每个 Thread 可能包含 `enriched_context`，提供结构化的问题信息：

| 字段 | 说明 | 来源 |
|------|------|------|
| `issue_type` | 问题类型（Bug/Security/suggestion） | Qodo Emoji 或 Sourcery Prompt |
| `issue_to_address` | 问题描述 | Sourcery Prompt |
| `code_context` | 代码上下文 | Sourcery Prompt |

### 问题分类

| 分类 | 类型 | 行为 |
|------|------|------|
| **必须修复** | Bug, Security, Rule violation, Reliability | 报告用户，等待修复指令 |
| **自主决断** | suggestion, Correctness, performance | 报告用户，自主决断 |

## CLI 命令

### 获取评论

```bash
python tools/manage_reviews.py fetch --owner OWNER --repo REPO --pr PR_NUMBER
```

### 列出评论

```bash
# 表格格式（默认）
python tools/manage_reviews.py list --status pending

# JSON 格式
python tools/manage_reviews.py list --status pending --format json
```

### 解决评论

```bash
python tools/manage_reviews.py resolve --thread-id THREAD_ID --type code_fixed
```

## 状态管理

### Thread 状态

| 状态 | 说明 |
|------|------|
| `pending` | 待处理 |
| `resolved` | 已解决 |
| `ignored` | 已忽略 |

### 解决依据类型

| 类型 | 说明 | 需要回复 |
|------|------|----------|
| `code_fixed` | 代码已修复 | 否 |
| `adopted` | 已采纳建议 | 否 |
| `rejected` | 拒绝建议 | 是 |
| `false_positive` | 误报 | 是 |
| `outdated` | 过时 | 否 |
| `manual_on_github` | 在 GitHub 上手动解决 | 否 |

## 常见问题

### Q: 如何区分 Qodo 的 Review Summary 和 PR Reviewer Guide？

**A:**
- "Review Summary by Qodo" = 代码变化摘要，不是改进意见
- "PR Reviewer Guide 🔍" = 改进意见摘要，需要处理

### Q: Sourcery 的 Reviewer's Guide 需要处理吗？

**A:** 不需要。"Reviewer's Guide" 是代码变化摘要，不是改进意见。应该处理的是 "Prompt for AI Agents"。

### Q: Qodo 的 PR Reviewer Guide 存储在哪里？

**A:** 存储在 Issue Comments 中，系统会自动获取。

### Q: 如何判断评论是否已解决？

**A:**
1. 首先检查 GitHub 原生状态 `is_resolved`
2. 然后检查文本标记：
   - Sourcery: `✅ Addressed`
   - Qodo: `☑`

### Q: enriched_context 为空怎么办？

**A:**
- Qodo 评论：从 `primary_comment_body` 解析 Emoji 类型
- Sourcery 评论：从 Prompt for AI Agents 映射
- Copilot 评论：默认为 "suggestion"

## 严禁事项

1. **严禁一次性解决所有评论**：每个评论必须单独处理
2. **严禁无依据标记解决**：必须先确认问题已解决
3. **严禁批量操作**：必须逐个评论处理
4. **严禁跳过说明评论**：rejected/false_positive 必须回复

## 相关文档

- [评论处理系统说明](评论处理系统说明.md) - 完整系统文档
- [AI 审查工具参考指南（归档）](archive/v1-ai-reviewer-guide.md) - 降级策略
- [fetch-reviews Skill](../../.trae/skills/fetch-reviews/SKILL.md) - Skill 使用说明
